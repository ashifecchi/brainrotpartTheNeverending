import java.awt.image.BufferedImage;
import java.util.ArrayList;

public class PlayableCharacter extends Character {
    public PlayableCharacter(String name, ArrayList<BufferedImage> sprites, ArrayList<BufferedImage> runSpriteslr, ArrayList<BufferedImage> runSpritesud) {
        super(name, sprites);
    }
    public runSprites()
}
